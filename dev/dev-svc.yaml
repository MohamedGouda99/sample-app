apiVersion: v1
kind: Service
metadata:
 name: dev-deployment-service
 namespace: dev
spec:
 type: LoadBalancer
 selector:
   app: dev-app
 ports:
   - port: 8080
     targetPort: 8080
     protocol: TCP