apiVersion: v1
kind: Service
metadata:
 name: prod-deployment-service
 namespace: prod
spec:
 type: LoadBalancer
 selector:
   app: production-app
 ports:
   - port: 8080
     targetPort: 8080
     protocol: TCP